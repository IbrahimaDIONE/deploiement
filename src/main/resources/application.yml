server:
  port: 8084

spring:
  application:
    name: ecom-site

  datasource:
    url: jdbc:mysql://localhost:3306/ecom
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create   # en local uniquement ; le VPS impose "update" par variable d'environnement
    show-sql: true

logging:
  level:
    com.jc_tr.ms_support: DEBUG
    com.jc_tr.ms_support.communications: DEBUG
    org.springframework.boot.devtools.autoconfigure.ConditionEvaluationDeltaLoggingListener: WARN
